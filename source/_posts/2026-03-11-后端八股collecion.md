---
title: 后端八股collecion
date: 2026-03-11 21:11:41
tags:
  - backend
---

many from AI

# MySQL



# Redis

## 定义
基于内存的高性能kv数据库，nosql

- 数据主要存放在内存，读写快
- 支持丰富的数据结构而不只是简单的kv
- 支持持久化
- 支持高可用和分布式，如主从，哨兵，集群
- 后端常用来做缓存，分布式协调组件

高性能内存存储+多种数据结构+丰富分布式能力

丰富数据结构
- string：缓存对象，计数器，分布式锁
- Hash：存用户属性等
- List：消息队列，评论列表
- Set：去重，共同好友
- Zset：排行榜，延时任务
- Bitmap：签到，活跃标记
- Geo：地理信息拆线呢
- Stream：消息流
持久化
- RDB快照；按时间点把内存数据写到磁盘
- AOF：追加日志，记录写命令
- 工程上两两结合，RDB快速恢复，AOF提高数据可靠性


## 为什么快
- 基于内存
- 单线程执行命令：避免锁竞争，死锁问题，编程简单，保证单命令原子性，CPU cache好，没有频繁线程切换。后来引入了IO多线程
- Io多路复用：一个线程就可以监听很多socket，处理大量客户端连接
- 数据结构设计高效，内部做了很多编码优化，比如小整数集合，压缩列表，哈希表，跳表等，针对不同场景选择不同底层实现，节省内存，提高效率
- 通信协议简单：自定义RESP协议，解析成本低，也支持pipeline，减少网络往返次数

redis底层是经典的reactor模式，事件驱动模型，内部有一个事件循环aeeventloop，处理两类事件
- 文件事件：网络读写，即客户端请求和响应
- 时间事件：比如定时任务，过期扫描，持久化等任务

注意，redis是串行的，如果执行某个命令很慢，比如key \*,大范围HGETALL，大key删除会阻塞整个线程。

Redis6引入了多线程IO，主要是把读请求和写响应这类网络收发工作交给多线程，但是底层的命令执行仍然是一个单主线程。

## 数据结构底层实现

string：最常见，底层是SDS，简单动态字符串，而不是c原生字符串。

- 记录长度
- 二进制安全，可以存图片，序列化数据等，c字符串有结束符。
- 减少缓冲区溢出风险
- 支持空间预分配，减少频繁扩容

list：redis早期用双向链表和ziplist，后来采用quicklist。quicklist我的理解是块状链表，每一块都是压缩连续内存块。链表插入删除方便，连续内存节省空间，减少指针开销

hash：底层实现会根据元素数量和大小做优化
- 元素少，小时，用紧凑编码节省内存
- 元素大，多，用哈希表

set：元素都是整数且不多，用整数集合intset，否则用哈希表

Zset：哈希表+跳表。根据member查score，根据score排序，范围查询。范围查询相对于平衡树稍微有好点，平衡树需要中序遍历

### 渐进式rehash

redis底层很多数据结构都是依赖于哈希表，key空间本身也是一个大词典。

哈希表扩容缩容，需要渐进式，避免阻塞主线程。
- 同时维护old table和new table
- rehash期间新数据写入new table
- 每次进行增删改查，顺便搬运一部分old table数据到new table，直到全部搬运完。

用时间换平滑性。

## 持久化

RDB：某个时间点将内存数据生成快照写到磁盘。
通过由fork出子进程执行BGSAVE，利用fork+写时复制，主进程继续处理请求，子进程负责生成快照。

fork的优点是子进程可以拿到父进程的当前内存快照，不会阻塞主进程很久，但也会有点抖动，写时复制会有额外内存占用。所以要避免在高峰期做重写，大快照，要预留足够内存，避免单个redis实例很大。

AOF刷盘策略:每条命令or每秒or操作系统决定。

AOF rewrite也是后台子进程，rewrite期间收到的新命令会放到AOF buffer和rewrite buffer，最后再合并。

## 多节点

主从复制，异步复制

从节点连接主节点，首次同步，如果调剂不满足增量同步则采用全量同步，主节点执行bgsave生成RDB发送给从节点，后续写命令直接从复制缓冲区发送从节点进行增量同步即可。

哨兵模式：主从架构下的自动故障发现和主从切换
哨兵节点会监控主从节点，发现主观下线和客观下线，进行主节点选举，进行故障转移。

集群模式：高可用+分片。
每个key通过CRC16算出槽位（一共16384）个槽位，每个节点负责一部分槽位，节点之间互相gossip 通信。

便于横向扩展，高可用，但运维复杂，多key操作，事务，lua在跨槽场景下有限制。


## 问题
### 缓存穿透
查询一个根本不存在的值，每次请求直接打到数据库

解法：
- 缓存null值
- 布隆过滤器
- 参数校验前置

### 缓存击穿
热点key失效，瞬间大量请求打到数据库

解法
- 热点key永不失效或逻辑过期
- singleflight/互斥锁重建缓存
- 熔断降级

### 缓存雪崩
大量key同时过期，流量大增打垮数据库

解法
- 过期时间加随机值
- 多级缓存
- 限流降级等

### 热点key
- 本地缓存分担
- 热点key拆分
- 读写分离
- 提前预热

### 大key
比如一个超大的hash，set，list等，会导致删除阻塞，网络带宽占用高，主从复制慢

解法
- 拆，控制大小
- unlink异步删除
- 定期扫描治理

### 持久化和主从切换

- AOF rewrite带来IO抖动
- 主从复制延迟
- 故障切换导致短暂数据丢失

### 内存淘汰
要明确哪些key不可以淘汰，避免丢失重要数据

## 多命令原子性


保证一组操作按顺序，不中断执行，场景有三种方式

- 简单批量执行，用 redis事务
- 需要CAS，校验后提交，用WATCH
- 需要读取，判断，写入整体原子执行，用lua
- 逻辑复杂，执行时间长，建议下沉到数据库，业务层，mq等，不要在redis做，阻塞redis主线程

### MULTI/EXEC 事务

- 客户端发送MULTI
- Redis把这个连接标记为事务态
- 后续多条命令入队而不是立刻执行
- 客户端发送EXEC提交-或DISCARD取消
- Redis按顺序执行队列中的任务，整个过程是串行执行


保证一组命令按顺序执行，不好被其他客户端插入。

不像MYSQL事务那样提供完整ACID
- 没用隔离级别
- 不支持回滚
- 已成功命令不会因为后续命令失败而自动撤销。后续命令不会因为前面执行报错而停止执行，而是继续执行。

错误分两种：入队阶段错误和执行阶段，前者全部指令都不会执行，但是后者可能是语法没问题但是数据类型不对，该指令会直接跳过。

更像是批量顺序执行机制


### WATCH + 乐观MULTI/EXEC 事务

可以理解是redis提供的乐观锁机制

在事务执行前，可以先监视一个或者多个key，如果这些key在exec前被客户端修改了，那么当前事务提交会失败，返回nil、空结果。业务侧可以做重试什么的

适合库存扣减，余额更新，配置版本控制什么的，类似CAS

但不适合高并发，高并发下冲突概率高，浪费资源，业务逻辑通常也不只有一个key

高并发更适合：LUA+mysql严格落库+MQ异步后续流程

### LUA脚本

- EVAL:直接传输脚本文件
- EVALSHA：传递脚本文件的SHA1，线上一般预加载脚本得到SHA1。

把多条redis命令和业务逻辑判断逻辑封装为一个脚本，在redis服务端一次性，原子性执行。

减少网络往返，业务逻辑可控，支持复杂原子操作。

并且可以使用失败直接中断redis.call或者失败继续执行，走异常分支redis.pcall什么的，

集群模式下，LUA脚本操作的key只能在同一个槽，

### 对比

原子性
- 事务：保证命令顺序连续执行，但不支持中间逻辑判断的完整原子性
- 脚本：整体原子执行，读写，判断都在一个原子单元

回滚
- 事务不支持
- LUA：没有传统回滚，但是可以自己业务逻辑实现

复杂逻辑能力：lua完胜
网络开销：事务要发送多条指令，LUA可以一次请求甚至预存脚本

易用性：简单批处理用事务，复杂条件更新用LUA



## 分布式锁

redis实现分布式锁是高性能，易落地的方案，但不是最强一致性的。

### 定义

分布式锁的目标是在分布式系统中让多节点互斥访问资源，比如库存扣减，定时任务抢占，幂等控制等。用redis实现，核心是利用redis单线程命令原子性，对某个key做抢占，抢到锁的客户端才能执行临界区逻辑

### 原理

加锁

- set key uniqueValue NX PX ttl，并且只有一条指令，保证了原子性
- NX就是key不存在才能加，实现互斥
- PX就是过期，避免客户端挂掉锁不释放
- uniqueValue一般就是UUID+线程ID+进程ID等，标识锁的持有者。这个ID不仅能实现互斥，也能实现幂等，因为自己可能会重复加锁

解锁
- 使用lua脚本保证原子性，要先比较value是否是客户端自己，避免误解别人的锁。比如线程A加锁后业务超时，锁自动过期；线程B加锁，此时线程A执行完业务直接DEL lock，把B的锁误删了。

续期
- 业务执行时间可能超过锁的过期时间，这时候需要看门狗机制实现自动续期。（使用LUA脚本检查锁是不是自己的，是自己的就续期）

获取失败要有兜底策略，比如降级，重试上限等。或许不使用重试，而是用redis的发布订阅机制等事件通知，避免无效轮询，但是实现复杂。

### 场景
- 单任务互斥：比如定时任务调度，多实例部署只允许一个实例执行
- 防止重复提交：比如订单创建，优惠券发放
- 库存扣减的串行控制：分布式锁+LUA、数据库条件更新、消息队列等
- 缓存重建互斥：缓存击穿时，只允许一个线程重建缓存

###  问题
分布式故障场景

主从切换导致锁失效(Redis复制通常是异步复制)
- 客户端在master加锁成果，但锁还没同步到slave，master就挂了
- 此时slave晋升为新master，此时这把锁不存在
- 客户端B可以在新master加锁成功，两个客户端都认为自己持有锁。
- 这说明Redis锁在主从异步复制的场景下，一致性有风险。


锁过期导致的并发执行(锁依赖于过期时间而不是租约的强一致协议)
- 如果业务执行时间超过TTL并且没有续期，会出现锁释放被抢走，出现临界区并发问题

时钟和暂停问题
- 比如 JVM FULL GC，GO的STW，机器卡顿，网络抖动，都可能导致续期不及时，甚至客户端暂停，锁过期了，客户端还在执行旧逻辑。因此线上要关注这些指标

Redis不可用时需要使用降级。redis不是严格意义的分布式强一致锁，而是带超时自动释放的分布式互斥标记

### 主从切换场景解法

#### RedLock
redis作者剔除的，思想就是部署多个redis实例，客户端要在大多数节点上获取锁才算成功，这能解决redis宕机或者主从切换导致的锁安全问题，

但仍存在争议，原因是仍然受网络分区，进程暂停，时钟漂移等问题影响，并且因为网络交互性能也有影响，并且redission也废弃了这个做法。

#### server高可用方案
redis配置文件有两个参数
```
min-slaves-to-write 1 默认是0
min-slaves-max-lag 10 默认是10
```
第一个是社渚主库最少有N个从库存活才能执行写命令，如果没有足够从库，主库就无法写入，减少数据丢失。

第二个是配置主从复制时ACK消息延迟的最大时间，可以确保主从延迟大时拒接写入。



#### 总结
- 业务只是工程上尽量互斥，redis锁足够，是轻量级互斥。
- 是金融扣除、强一致主从切换、分布式事务协调等核心场景，建议用zk，etcd这种基于共识协议的协调组件。

可以降级用zk锁，从CAP上说，Redis集群倾向于AP（高并发），Zk集群倾向于CP高可用。因为ZK主节点写成功后，还要同步给至少半数从节点才会响应成功给客户端。如果ZK主节点挂了，ZK的ZAB协议就能保证数据同步完成的节点被推举为主节点，所以ZK不会发送分布式锁失效的场景。

# MQ



[消息队列设计精要 - 美团技术团队](https://tech.meituan.com/2016/07/01/mq-design.html#:~:text=消息队列设计精要- 美团技术团队)




## 定义

异步通信中间件，生产者发送消息到Broker，消费者异步消费

解决的问题不是传数据，而是解耦调用链，缓冲流量，提升系统可恢复性。

核心是发布订阅模式，只要能解耦，都是消息队列

- 内存队列
- 消费者生产者TCP直连长连接，去中心化
- 生产者写本地磁盘文件，消费者顺序读本地文件，特别是日志场景
- mysql的消息表



topic是逻辑概念，对消息根据业务进行分类，topic内拆分为多个partition，提升吞吐。

消息队列本质是RPC（俩次）+存储系统



## 原理

- 生产者发送消息到topic/queue,Broker持久化存储，消费者按组消费
- 交付语义：常见是至少一次，所以业务侧必须做好幂等


### 投递语义

生产->Broker->消费
三种投递语义，本质是这三者失败重试，ack提交，持久化下达到的情况

至多一次
- 常见做法是先提交消费位点，确认，再处理业务
- 如果处理业务前宕机，消息就丢了
- 优点是不会重复，但可靠性差

至少一次
- 常见是先处理业务，处理成功再ack,提交offset
- 如果处理完还没来得及ack提交就宕机,Broker会重投，所以会顺序消费
- 这是最常见最现实的模式

恰好一次
- 消息只被成功处理一次，难点不在Broker，在于业务副作用
- 消费ack提交丢了，可能会导致业务重复执行。要实现恰好一次，就需要将消息消费确认和业务状态变更组合为一个原子语义操作，这在分布式环境里成本很高，涉及到有状态的写磁盘

大多数情况下都说至少一次，因为网络抖动，消费者宕机重启，ack丢失，重复投递是很常见的。

日志采集，监控埋点，非核心通知这些可以使用至多一次，因为丢一点无所谓，不希望重复造成噪音

关键业务，就需要至少一次，不可丢失，业务幂等来兜住。

恰好一次一般是适合流式计算，特定闭环环境，通过事务/checkpoint可以做到端到端较强语义。但涉及到外部DB，第三方接口等这种有副作用的系统，恰好一次就很难

## 性能

mq快，不是因为它功能简单，而是它在数据写入模式，网络模型，存储模型等设计成了适合高吞吐的形态。

快，一般就是高吞吐和低延迟了，大部分mq都说倾向于把吞吐做高，在可靠性和实时性之间平衡，在部分指标上很快



### 顺序写磁盘

最关键的一点，避免随机IO
Kafka rocketmq本质都是append-only日志模型，消息都说顺序追加到文件末尾

对比数据库，需要维护树索引，页分裂，随机更新，成本高

### 批量处理

- 生产者可以批量发，Broker可以批量刷盘，消费者可以批量拉
- 批量将多次syscall，网络往返RTT，磁盘刷新合并调

### 异步刷盘/Page Cache

很多MQ不是每来一条消息就fsync到磁盘的，而是先写到OS Page Cache，OS会执行异步刷盘。

这样写入很快，因为内存写比磁盘写快多了，本质是在性能和可靠性做权衡


### 零拷贝

Kafka这种，消费会大量使用零拷贝，如sendfile

数据可以直接从磁盘文件/Page Cache发到网卡，减少内核态用户态之间多次拷贝，这样CPU消耗更低，吞吐更高。

像GO，C++的传递指针，传递引用，大结构体大数组不要拷贝

### 数据结构简单：顺序读写
MQ核心就是追加写和按照offset顺序读，功能简单，路径短

不需要像mysql那样，关系型数据库那样支持复杂查询，事务隔离，join等


### 顺序消费模型 弱化复杂事务
消费端按照队列，分区顺序批量拉拉取，这种读模式对磁盘预读友好，缓存命中率高



### 总结

Kafka 为什么快
- Partition 本质就是一个顺序追加日志文件
- 生产端批量写入
- Broker 依赖 Page Cache
- 消费端按 offset 顺序拉取
- 支持零拷贝发送
- 所以非常适合高吞吐日志、埋点、流式数据场景

RocketMQ 为什么也快
- CommitLog 顺序写
- ConsumeQueue 做轻量索引，加速消费定位
- 支持刷盘和复制策略权衡性能与可靠性
- 适合业务消息、事务消息、延迟消息等更偏业务中台场景


RabbitMQ 为什么吞吐通常不如 Kafka
- RabbitMQ 路由能力更强、协议更复杂、功能更丰富
- 但它在极致吞吐上通常不如 Kafka 这类日志型 MQ
- 换句话说，RabbitMQ 更擅长灵活性，Kafka 更擅长大吞吐

还有异步刷盘，机器掉电挂了，pagecache里的会丢

mq分区热点也会有瓶颈，要合理分区

性能瓶颈很多情况下都在消费端，比如频繁DB交互，外部接口，导致消息堆积。

## 场景

- 异步解耦：下单后的积分，优惠券，短信，推荐系统等，主流程只落订单并发送MQ消息，主链路更低，接口返回更快，非核心下游故障也不影响主流程，主流程发一个MQ事件，不同消费者各自订阅处理

- 削峰填谷:上游做限流，库存预扣，然后请求写MQ，下游服务匀速处理，保护DB和核心服务

- 最终一致性：订单支付成功，需要更新订单状态，账户流水等多系统，这大概率是跨服务场景，一般用本地事务+MQ，靠重试和补偿实现最终一致性，而不是分布式事务。要求更高用事务消息/outbox模式

- 延迟，定时任务：订单超时取消，用MQ的延迟消息or时间轮，定时任务+MQ等，任务量大要关注消息堆积和时间精度。

- 广播：用户资料变更，触发缓存刷新，索引刷新等。这是事件驱动，系统扩展性友好

可以当异步RPC ）

消息队列不是万能的，对于强事务保证和延迟敏感，RPC更合适



## 怎么用

### 生产

- 定义消息体：业务主键，事件类型，时间戳，版本号，traceID
- 发送方式：
	- 非关键链路直接异步发送
	- 设计数据一致性采用事务消息或outbox
- 发送要有成功和失败处理

Broker/Topic设计
- 按业务拆分topic
- key一般用订单号，用户id业务键，便于顺序消费和排查
- 重要消息要持久化，多副本，避免Broker挂
- 提前规划好分区数，分区少吞吐不够，分区多，管理和消费者负担大

### 消费

- 消费逻辑必须幂等，网络抖动，消费重试，Broker重投都会导致重复消费
- 幂等做法
	- 业务unique key去重
	- 数据库唯一索引
	- Redis去重
	- 状态机规则校验
- 消费成功再提交offset，失败进入重试，超过阈值进入私信队列
- 消费要限流，线程池隔离，避免打满DB连接池

### 保障

- 区分可重试异常和不可重试异常，不能无脑重试
- 要看死信队列，能回放
- 监控：消息挤压，生产失败率，消费失败率，重试次数，死信数，消费延迟
- trace链路追踪

## 选哪个
选型依据
- 吞吐
- 顺序
- 事务消息
- 运维成本



## push模型与pull模型对比

工程落地，大吞吐选择pull；简单实时选push。

成熟mq都是以pull为主，体验上类似push



### 定义

push：Broker主动推消息给Consumer

pull：消费者主动向Broker拉，比如Kafka

很多中间件表面是push，底层是长轮询pull

### 原理

push：Broker维持消费者连接，订阅关系，消费能力状态，一有消息就主动尝试投递，还要处理限流，重试，堆积等

pull：消费者自己把握拉去节奏，自己选队列，offset，条数。每消息可以短轮询立即返回也可以长轮询

### 场景

push：实时性高，消费者少，消息轻量，SDK简单,订阅即消费，比如IM，通知，告警

pull：高吞吐，大数据量，消费者自己控制消费速率，不同消费者能力差异大，可以做扩展和被压，比如日志采集，埋点，流式计算等

### 优缺点

- 实时性：push直接，延迟低；pull短轮询会因为拉去间隔导致延迟，长轮询也能做到接近push的低延迟。轮询的间隔该这么定，业界流行的算法就是从短间隔逐渐增长到长间隔，一有消息就回退到短间隔。如果消息间隔很长，还是用长轮询，但海量的block连接开销还是不小，要合理评估。

- 吞吐能力：push的broker需要主动调度，投递，管理大量consumer状态，复杂度高；pull可以批量，Broker模型简单，高吞吐

- 留空与背压：push的Broker不容易指导consumer状态，能消费多少，会不会爆。push由消费者自己决定，pull更好

- 复杂度:push使用者简单，Broker复杂（连接管理，消费者状态，重试，流量控制） ； pull：broker简单，consumer sdk复杂。

- 消费公平：push Broker负责公平分配消息，pull 的consumer主动拉，天然适合按能力消费，异构能力消费，按需消费，push也容易出现消息堆积，因为可能会乱push消息，consumer不合适

- 空消息开销：push有消息才推，不存在。  pull短轮询会频繁拉空，浪费网络和Broker QPS。可以采用长轮询优化

- 顺序消费：全局顺序，可能就日志这种合适了。pull下单produce对单partition就可以实现。但是push就麻烦了，broker来负责全局顺序唯一，本身还要push也容易堆积。

  



## 问题/风险

### 消息丢失

- 生产端没发送成功，Broker没持久化，消费端处理了但没正确提交
- 生产确认，Broker持久化副本，消息确认机制
- 关键消息还需要做业务补偿和消息回查，不能只依赖中间件

还得需要补偿兜底机制。



#### 生产端保证不丢

为什么丢：业务成功了但是没发出去。发出去了但是不知道Broker收到了吗

- 关键消息采用同步发送，等待Broker返回ack。Kafak有acks=all，RabbitMq有publisher comfirm，Rocketmq也有发送结果确认
- 发送失败要有重试，注意幂等和次数上线，区分普通失败和业务异常
- 本地事务和发消息一致性：核心链路不能靠业务提交后发mq，而是采用事务消息/Outbox模式,极端情况下可以用消息表本地兜底

#### Broker保证不丢

消息收到但是没落盘挂了，单副本机器挂了。

持久化：要写磁盘

- RabbitMq要开启durable queue和persistent message
- Kafka天然日志持久化模型，但是仍然看刷盘和副本配置
- RocketMQ也有同步异步刷盘的选择

多副本：topic一定要多副本，Kafak通常配置副本因子>=3，主副本写成功后，同步到足够多副本再返回ack

高可用：主从切换哪些，副本同步策略，选主策略哪些

#### 消费端

接到消息，要业务执行完才能返回ack，commit offset。

DB超时，网络波动，锁冲突要允许重试，超过次数上限入死信队列。

消费端做好幂等，状态校验



### 重复消费

- 工程上默认常见的，所以消费逻辑一定要幂等，必须项

### 消息积压

- 消费者性能下降，DB变慢，分区热点，代码bug
- 止血：扩容消费者，临时限流，跳过非关键逻辑，再定位瓶颈
- 积压严重，还需要评估消息是否过期，是否需要降级消费

### 顺序消费

- 全局顺序成本高，一般只保证局部顺序，比如同一订单，同一用户维度
- 通常按业务key分区，让同一个key落到同一个队列

### 消息乱序和回滚

- 比如订单取消先于创建订单消息被消费，就会导致状态异常
- 状态机校验，版本号控制，必要情况下用串行消费

### 事务一致性

- 本地事务成功，但是消息发送失败
- 消息发送成功，但事务执行失败回滚。

- 强依赖MQ能力，希望链路更实时，采用事务消息
- 更通用，对中间件依赖更小，更稳，用outbox



事务消息：MQ提高的一种两阶段能力，让本地事务执行结果和消息是否对消费者可见绑定起来



生产者发送一条半消息给MQ，MQ会先收下不会立即投递给消费者。生产者执行本地事务，将事务执行结果告诉MQ，MQ最终来决定这条消息是否要投递给消费者。MQ也可能会回查本地事务状态。



RocketMQ

适合支持事务消息的MQ，业务实时性高，业务能接受对特定MQ产品能力有依赖,业务开发负担小。但本地事务回查逻辑复杂，一旦MQ、事务状态回查机制处理不好，很难排查。



outbox模式：业务数据库增加一张消息表，把业务数据和待发送消息放在一个本地事务提交。后续再由后台任务或者CDC程序投递给MQ。总结：消息落库，异步投递

更通用，以数据库为实施来源，便于审计补偿追踪，人工介入。有一定延迟和维护成本，设计成本。

扫表太多，单表太大，并发重复投递都是潜在的风险



#### outbox扫表设计

扫太猛，数据库崩溃。

扫不快，消息延迟堆积

##### 表设计

- id
- biz_key
- event_type
- payload消息体
- status:new sending sent failed
- retry_count
- next_retry_time等
- 分片字段等...

索引就(status,next_retrytime,id)来批量拉取消息，还要biz_key来辅助业务等

核心原则扫表必须命中索引

单表太大要归档和分表。

读写分离慎用，主从延迟任意导致刚写入的消息扫描不到和状态混乱

sending太久可以回退new

加各自监控统计

##### 扫描策略

- 定时
- 小批量limit
- order by id
- 扫new | 到了重试时间，即可以发送的消息

量大可能需要多worker并发，但是需要处理重复抢占的问题，用版本号，乐观锁等

扫描频次要动态调整，可以根据具体情况，如堆积时调高。



以下是高级的扫表方案：

扫描甚至可以搞成事件驱动，比如写完outbox，发个轻量级通知给内存队列/redis stream等，通知扫描任务执行。这样扫描就是“事件驱动+定时兜底“

或者用CDC方案订阅mysql binlog，监听新增记录来定时投递。延迟低，但是复杂

分片扫描，每个扫描只负责一个分片，减少热点和锁竞争



##### 并发控制

多worker同时跑，如果先select再发送，不是原子，很容易重复发送



- 抢占式：先抢占再发送，哪个实例成功更新记录为sending状态，则负责对它的发送
- select for update skip locked。数据库版本支持可以使用，如mysql8，多个worker可以并行拿到不同数据

一般用抢占式，比较通用可控



##### 发送和状态诙谐

扫描拿到后负责投递和回写成功/失败给数据库。

注意不要长事务，短事务抢占+无事务发送mq+短事务回写

可以批量回写

##### 重试机制

指数退避和重试上限。告警机制和补偿机制



## Design a MQ

### 目标
先定目标，再分层设计

是要偏业务消息，还是偏日志流，这决定后面很多设计的取舍，下面是比较通用的

- 需求
- 整体架构
- 存储模型
- 生产消费模型
- 高可用与一致性
- 可靠性与语义
- 扩展性
- 运维监控
- 风险与权衡

### 需求分析

功能目标

- 生产，存储，消费
- 支持topic/queue
- 支持消费组
- 支持重试，私信，延迟消息
- 支持顺序消息和普通消息

非功能目标

- 高吞吐
- 低延迟
- 高可用
- 消息不易丢失
- 可水平扩展
- 可观测，易运维

语义目标：默认支持至少一次，可以通过幂等实现业务上近似恰好一次

### 整体架构

- 客户端层：生产者，消费者sdk，负责序列化，重试，路由，负载均衡，ack，offset提交等
- 接入层：Broker节点集群，接受生产请求，持久化消息，响应拉取请求，做消息存储和转发的核心
- 元数据层：管理topic，partition，消费组，Broker注册，主从信息，路由信息。  尽量只存元信息，不承载消息数据，类比为NamServer/controller等
- 运维控制层：管理后台，监控报警，权限控制，消息查询，消息回放，死信队列，限流配置等

核心设计原则：

- 元数据和数据面分离
- Broker无状态一部分逻辑，存储仍在Broker本地
- 客户端尽量直连Broker，减少中心节点转发瓶颈

### 存储设计

MQ核心，采用顺序追加日志模型

- Topic->Partition：每个Topic拆分为多个Partition，Partition是吞吐和并行度的基本单位，一个消息只落在一个Partition内，Partition内有序
- 日志存储：每个Partition对应一个追加写日志文件，只做append不做随机修改，消息写入按offset递增
- 索引设计:最简单的按offset直接定位，如果要按照时间查，就加给个时间索引。 如果需要按业务key查，可以加个轻量二级索引
- 文件分段：按照大小/时间分段，1Gb一个segment，便于滚动，清理，归档，恢复
- 保留策略：按照时间/容量保留，过期segment异步删除

### 生产模型

- 路由：生产者根据topic路由到某给partition，可以轮询分区，可以按key hash到某个partition，hash方法可以保证同key有序
- 发送模式：同步发送，可靠性高；异步发送吞吐高，单向发送最轻量，但可靠性最差。
- 批量发送：生产者批量发送消息，降低RTT成本和syscall成本
- 幂等生产：生产者给消息分配唯一ID，序列号。Broker可以做去重，减少网络重试导致的重复写入。（全局去重成本很高）
- 压缩：大批量消息可采用压缩算法，提升带宽利用率，但提高CPU开销

### 消费模型

- 选择pull而不是push：消费端自己控制速率，方便做背压；Broker压力可控；批量拉取高效；Kafka这种高吞吐MQ本质也是拉

- 消费组：同一个消费组内，一个Partition同时只能分配给一个consumer，不同消费组独立。保证partition内顺序消费
- offset：每个消费组维护自己的消费位点，可以Broker统一管理。
- Ack机制：业务成功后提交offset，默认至少一次，如果消费失败，等待重试。
- 重平衡：consumer上下线，需要重新分配Partition，为了减少抖动，采用增量重平衡而不是全量打散。

### 高可用与一致性

高可用下的数据一致性

- 副本机制：每个Partition采用一主多从，请求先到主，再复制到从
- 写入确认策略：弱一致：主落盘立即返回；强一致：主+多副本确认返回。可以灵活配置
- 选主：从同步状态好里的从里面选主，状态差的副本不能参与
- 元数据一致性：元数据采用Raft这类一致性维护Topic，Partition，Leader信息，保证控制面状态一致

数据面不做分布式强事务，Partition采用主从复制保证可靠。控制面用强一致协议保证数据可靠



### 可靠性与语义

默认至少一次

- 生产端：Broker ack后才确认成功；超时重发；可选幂等生产
- Broker：持久化，多副本复制，崩溃根据日志重建
- 消费端：先处理业务才返回offset，失败重试，超重试阈值入死信
- 延迟消息/重试队列：消息失败入延迟重试Topic，指数退避什么的，避免瞬时故障导致大量重试
- 死信队列：多次失败的消息单独隔离，支持人工排查,回放

要实现恰好一次，Broker可以承担一部分幂等和事务，但全链路很难，业务还是靠幂等和补偿



### 扩展性

- 水平扩展：Topic可以扩Partition，Broker可以横向扩容，Partition可以再Broker之迁移
- 热点Partition：提前对key打散，业务拆分，扩容热点Partition
- 大消息治理：MQ不适合放大消息，必须做消息size限制，可以存大消息引用，消息本地放在OSS
- 顺序性权衡：全局顺序不可能高性能，只能保证同Partition内顺序

### 运维监控

- 生产侧：发送TPS，发送成功率，发送延迟P99
- Broker测：写入吞吐，磁盘利用率，Page Cache命中，网络监控，副本同步延迟，leader分步是否均衡
- 消费侧：消费TPS，消费成功率，消费积压量，最老消息年龄，Consumer重平衡次数
- 系统治理能力：Topic管理，ACL权限管理，消息回访，消息轨迹，死信重投，限流，配额管理

### 风险与优化

- 脑裂：需依赖强一致元数据层控制
- 积压风险：需限流，扩消费组，监控最老年龄
- 磁盘打满：MQ本质是存储系统，磁盘是高风险点，需有保留策略，容量告警，自动清理
- 重平衡抖动：consumer频繁上下线会导致Partition不断迁移，要有稳定的分配策略和心跳机制
- 顺序与吞吐冲突：顺序性与并行度相悖，默认采用partition内顺序

### 总结

关注点

- 消息怎么存：顺序追加日志
- 消息这么分片：Topic+Partition
- 这么高可用：多副本，一主多从
- 这么保证不易丢：持久化+ack+重试
- 这么消费：拉模型+消费组+offset
- 这么扩展：partition扩容，Broker横向扩容
- 这么运维：积压，延迟，死信，回放，限流等

顺序日志，Partition为可扩展最小单元，副本机制，消费组和offset实现解耦消费


# 其他



## GC

垃圾回收：识别不再被程序使用到的内存对象并回收，避免内存泄露和手动释放带来的错误

### 定义

编程语言运行时提供的一套自动内存管理机制，程序申请出来的堆内存，如果后续没有有效引用指向它，这块内存就会被回收。

比如java go py js
c和c++更多的是手动管理了，现代c++也会使用RALL，智能指针降低手动管理成本

### 用处
解决以下问题
- 内存泄露：对象没用了，但是没释放
- 野指针/重复释放：手动管理容易free错
- 开发效率：业务开发不需要过多关注对象生命周期

本质是用运行时成本换开发效率和安全性。可能引入停顿，内存占用更高，出问题排查更困难

### 基本原理
核心问题只有两个
- 哪个对象还活着
- 怎么回收死掉的对象，并尽可能减少碎片和停顿

怎么判断活着，主流两种思路

- 引用计数法：实现直观，回收及时，但维护计数有性能开销，解决不了训循环引用
- 可达性分析（主流）：从一组GC Roots（栈上局部变量，方法区/静态变量、常量引用、活跃线程引用的对象等）出发，沿着引用关系搜索，能到达的是对象认为是存活对象，不能到达的就是垃圾。

### GC算法

- 标记-清除：先标记存活对象，再清除未标记对象。实现简单，不需要移动对象；但会产生内存碎片，大量小碎片影响后续分配
- 复制算法：把内存分为两块，每次只用一块。GC时将存活对象复制到另一块，然后把原来整块清空。优点是没内存碎片，分配快，通常是指针碰撞。缺点是空间利用率低，如果存活对象多，复制成本高，适合对象生命周期短的，比如新生代
- 标记-整理：标记存活对象，把存活对象往一端移动，直接清理边界外内存。没碎片，空间利用率比复制高。但移动对象时候还需要维护引用关系，成本更高，GC暂停时间长，可能要扫描多次
- 分代回收：工程最常见，不是单一算法。核心假设是大多数对象很快就会死，少数的才会长期存活，将堆分为不同代（根据存活时间划分，新生代，老年代，永久代）。新生代对象刚刚创建，可能很快就死了，回收频繁，适合复制算法。老年代存活时间长，可能越活越久，适合标记-删除或标记整理算法。

### 关键问题

核心是降低回收对业务的影响，特别是STW。GC某些阶段需要暂停用户线程，防止对象引用关系在扫描过程中不断变化。

如果停堆太久，线上就会：服务RT抖动，延迟升高，高并发场景请求超时，MQ消息堆积。

现代GC优化方向通常就是减少停顿时间，并发，增量，分代回收。减少CPU消耗

### 不同语言GC特点

java
- GC体系化，分代思想成熟，有多个收集器
- 重点是吞吐量，停顿时间，堆大小之间做平衡
- 有Minor GC，major gc，full gc，cms，g1，三色标记等常见面试问题

go
- 并发GC，设计目标更倾向于低延迟而不是极致吞吐
- 采用三色标记+混合写屏障等机制降低STW
- 常见问题是分配太频繁导致GC压力大。所以要减少临时对象创建，对象复用（如sync.Pool),避免逃逸到堆上，关注GOGC，GC pause，heap growth

py
- 引用计数+周期性回收机制

### 问题

- 对象分配过快，导致新生代gc频繁
- 大对象太多，直接进入老年代
- 内存泄露，导致old gc、full gc频繁
- 不合理缓存，导致堆占用大
- 批处理一次太多，瞬时创建大量对象
- 日志拼接，json序列化产生大量临时对象
- 连接池，线程池，map使用不当导致对象无法释放

大概就是看GC日志，堆使用情况，哪些函数分配多，是否有缓存为淘汰，静态集合，线程本地变量，goroutine泄露

### GO GC

[Go 垃圾回收器指南](https://colobu.com/2022/07/16/A-Guide-to-the-Go-Garbage-Collector/)

#### 特点

- 无分代：分代GC依赖于分代假说，分代GC主要回收目标是在新生代，而非频繁检查所有对象。而go编译器会进行逃逸分析，大部分新生代对象都会放在栈上，当goroutine死亡时栈也会回收，不需要GC。分代没优势

- 不整理：整理算法的有点是无碎片，可以顺序分配。但是go使用tcmalloc内存分配算法，基本没碎片问题

- 并发（与用户代码一起进行）。go目标是低延迟gc，减少stw，减少大对象和分代带来的影响，更好的与用户协程并发

#### 过程



- STW，扫描根对象，开始标记
- 并发标记
- 标记终止，短暂STW
- 并发清扫
- 下一轮分配期间继续清扫或辅助清扫。



1. 标记准备
进入一个很短的STW，进行状态切换和根集合准备，也便于插入写屏障
- 如全局变量
- 各goroutine栈上引用
- 一些运行时管理结构里引用
- 寄存器值等

2. 并发标记：恢复用户goroutine标记，同时后台标记线程开始从根对象开始遍历对象图，标出存活对象。这个阶段，一些分配内存过快的goroutine可能被停止，转去做辅助标记。go的gc会导致内存分配有点慢，这也是社区诟病的一点。

3. 标记终止：接近标记完成时，再来一次短暂的STW，处理收尾，关闭写屏障，保证这一轮标记结果一致

4. 清扫：回收没标记到的对象，go gc很多时候是并发进行的，不一定是一次性全堆完成，可能是其他goroutine尝试申请内存时触发清扫，将清扫的延迟和吞吐下降打散到每次内存分配。

感觉类似DRAM的刷新算法，因为DRAM电容会漏电，一定周期内需要进行充电，统一刷新会占用一段时间，导致内存一段时间内不可用。但是如果分散，均匀的多次刷新，延迟抖动就比较小，实时性强等。

####  三色标记

- 白色：未访问到，理论最后如果还是白色，就会被回收
- 灰色：已经找到该对象，但它引用的子对象还在扫描
- 黑色：该对象及其子对象已经扫了

- 一开始所有对象都是白色
- 从GC roots触发，扫到的对象放入灰色
- 不断从灰色集合中取出，把它引用的白色染成灰色
- 当前对象扫完，染成黑色
- 当灰色集合为空，所有能到达的对象是黑色的，白色就是不能到达

#### 并发标记存在的问题

Go gc是并发gc，gc在扫描时，业务goroutine也在不断修改对象引用关系，这可能导致漏标

1. 黑->白：A对象已经扫描完标记为黑了，然后业务代码给它增加了一个白对象的引用。这个白对象就可能扫不到了，最终被错误回收。

2. 灰 --!->白:灰对象到白对象的路径，gc还没来得及扫描，就被业务goroutine断掉了。

所以并发标记必须搭配品璋机制，保证不该死的对象不会被错误回收，不会被漏掉

#### 写屏障

[Golang垃圾回收(GC)介绍 | Random walk to my blog](https://liangyaopei.github.io/2021/01/02/golang-gc-intro/)

- （强）三色不变性：不能存在黑---->白,否则白对象会漏标。
- （弱）三色不变性：黑色可以引用白色，但该白色必须存在灰色对它的引用，或者链路上存在灰色，有灰色访问到它即可。

这两不变性分别对应上面两种情况，只需要满足任意一种可变性就能保证gc正确性。go通过写屏障保证并发标记下对象图变化不会破坏三色不变性，不使用stw。




程序在执行修改指针引用这类操作时，runtime额外插入的一小段判断逻辑(可以叫做赋值器，黑色赋值器和灰色赋值器），满足条件时触发回调，以维护gc正确性。不是普通字段的写入，而不是可能影响对象图可达性的指针写入操作

- 插入屏障（Dijkstra）：新增引用，当黑对象新增白对象引用时，会将白对象标灰，防止漏标。实现了强三色不变性，可以立刻投入使用。缺点是这相当于指针赋值都需要引入屏障，会带来性能开销，因此go团队觉得栈上对象不用屏障机制，栈上发生操作只是用灰色赋值器，最终标记终止阶段STW再统一扫描栈，影响性能。
- 删除屏障（Yuasa）：删除引用时触发，灰色对象对白色的引用，当引用关系修改或者被删除，把旧对象也保护起来（把白对象染灰），避免因为路径断开导致漏标。实现了弱三色不变性。缺点是一个对象被删除，可能会被标灰而活过了这一轮，等待下一轮回收，这样扫描精度有所下降



后来go的新版本，综合两种思想，形成了混合写屏障，实现了弱三色不变性。

- 混合写屏障：综合两种思想，兼顾性能和正确性。
  - GC开始时将栈上对象标记为黑（不需要再STW然后扫描了）。
  - GC期间，栈上新增对象均为灰
  - 被删除/被添加引用的对象均标记为灰色






业务代码改引用，运行时要么把白对象提前标灰，要么把相关对象保守标记，保证不会误回收错误对象。

#### 为什么需要STW

STW实现全局一致快照和完成阶段边界切换

1. 扫描根对象需要一致性视图：比如gorouine栈，全局变量这些根集合需要在一致性时刻扫描，否则边扫描边变很困难

2. GC阶段切换需要全局同步：普通运行态到标记态，标记态到标记终止态，需要让所有GMP进入某种统一状态

3. 栈重新扫描或者栈屏障处理：goroutine栈是动态变化，里面有大量指针引用，某些适合需要STW配合做栈相关处理，保证根扫描正确

4. 运行时元数据处理：有些GC元数据更新，工作队列切换，清扫状态推进等，要求全局短时停顿保证一致性

#### GO STW为什么短

因为把大部分重活放到并发阶段
- 大部分对象标记是并发标记
- 清扫也尽量并发
- 写屏障保证并发期间引用变更可控
- 栈扫描，终止阶段做了很多优化

目标是短暂停顿+一定CPU开销，和追求高吞吐，允许较长停顿的策略不同

#### GOGC是什么
一个核心调优参数，控制堆增长到什么时候触发下一轮gc，默认是100（百分比），当堆增长了多少时再触发一次gc。本质是在gc频率和内存占用做取舍



gogc小：gc频繁，内存占用低，CPU更多时间花在gc上，可能影响吞吐
gogc大：gc少，内存占用高，cpu压力小，但堆更大，单论gc扫描对象更多



还要个runtime.GC()可以手动触发），还有定时触发什么的



go程序有空闲内存还会返还给os，可以设置至少保持多大内存，减少向os申请和回收内存。

#### GC问题
1. 分配频率过高
- json序列化创建大量临时对象
- 字符串拼接过多
- map/slice反复扩容
- 请求创建大量短命对象
- fmt，反射，interface装箱额外分配

2. 逃逸过多
- 返回局部变量指针
- 闭包捕获变量
- interface转换
- 反射
- goroutiine异步引用外部变量

3. 大对象
- 一次性读取大量文件，大buffer，大map，slice。导致gc成本高，堆膨胀，内存碎片等

4. goroutine泄露
- goroutine栈不大，但是如果栈里持有大量对象引用，会让对象无法回收。
- channel无人消费导致阻塞
- context没取消
- 没超时控制等

5. 不合理缓存
本地map缓存无限增长，没用ttl，lru，大value常驻，gc每次都要扫描它

#### 怎么优化

需要关注的指标

- CPU利用率：垃圾回收是cpu密集型
- GC造成的停顿和频次：STW和辅助标记阶段
- GC可扩展性，当堆变大时，GC耗时增长情况



代码，数据结构，参数，排查

1. 减少内存分配，这是最有效的
核心在堆分配
- 减少临时对象分配
- 减少新建goroutine，也不是新建，就是打散，分多次建（使用WaitGroup来实现），你一次性建太多，给调度器增大压力，也并不能提高处理性能。
- 复用对象，必要用sync.pool。复用buffer，序列化对象
- 预分配slice,map,减少扩容
- 少用fmt.sprintf，日志字段避免过度格式化
- 避免频繁[]byte和string转换
- 热路径少用反射
- 减少逃逸

2. 控制对象生命周期和引用
- 大slice，map用完及时置空。还有小slice引用大slice
- goroutine推出前释放上下文相关资源
- 避免全局变量，长生命周期对象持有短生命周期对象
- 缓存系统是否有容量上限，ttl，大value，热点key，是否需要改redis


3. 谨慎使用syncpool
 适合复用短期临时对象，比如中间结构体，编解码对象，bytes.buffer。

但大对象，池太大不合适，gc时pool中对象也可能被清理

4. 逃逸分析
go build编译时可以看哪些逃逸了，为什么逃逸，如何优化

5. 参数调优
- 调整GOGC
- 升级go

#### 排查
1. 看现象
- RT抖动
- CPU升高
- 内存持续上涨
- OOM
- goroutine数量异常

2. 看基础指标
- GC次数
- GC puase
- 堆对象数量
- 分配速率
- 堆大小
- goroutine数

3. pprof
- 看谁持有内存，分配最多，泄露和阻塞，GC cpu占比

4. 定位热点路径：看是那段代码问题，大量对象创建，goroutine泄露，逃逸等 



#### 绿茶

[Go 官方详解“Green Tea”垃圾回收器：从对象到页，一场应对现代硬件挑战的架构演进 - Tony Bai](https://tonybai.com/2025/10/31/deep-into-go-green-tea-gc/)
- gogc是标记-清除算法，go堆内存是一页页分配的，每页划分为只存不同的固定大小的内存块，可以用一个bitset表示该小块内存是否访问了
- 垃圾回收中，大部分时间都是在标记阶段，因为图遍历算法在堆内存中跳来跳去，cpu缓存利用率不高。图访问在内存中是无序的且是每一步都依赖于前一步，依赖性强，CPU微指令也无法优化（异步访存，先去干其他事情），标记阶段都是执行很多无规则的但小块简单的工作
- CPU核心多了，并行度看似提高了，但内存总带宽没上去，扫描用的共享对象队列可能成为瓶颈

扫描是一个个对象地扫，工作队列是先进后出的栈，类似深度优先搜索，递归地扫，可能在不同页之间跳来跳去，



go新版本引入了绿茶算法
- 不再扫描具体对象，而是扫描整个页，跟踪整个页。扫描完仍然需要对对象进行标记，但是会跟踪每个页本地标记的对象，而不是跟踪整个堆中标记的对象
- 每个页原版就一个bitset，现在翻倍了，两个，一个表示seen，一个表示scanned。
- 把整个页放到工作队列上，而不是之前的单个对象。
- 扫描一个页，就是对这个页中seen为真但是scanned为假的对象进行扫描（可以一次扫同一个页里面的多个对象），这可能扫描到别的页的对象，将别的页放入到工作队列等待扫描即可。一个页可能会被重复放入工作队列，和之前的一个个对象扫描只会放入一次不同。

这样扫描类似于bfs，让同一页多个seen对象一次性扫，提高了cpu缓存利用率。只跟踪页面，工作队列也能变得更小，原本是栈结构，递归栈可能会很大，工作队列小，CPU竞争可能更小，更快了。



特别是那种比较规整的页，比如图结构看起来像是完全二叉树那样，这样就能大大加快扫描速度。如果是类似菊花图那样的结构，那可能因为需要维护两个btiset，累积多个待扫描对象到一个页，反而做了更多工作，性能下降。但实际测试，只需要累积单个页扫描对象，就能比原来的扫描算法快上了。



现代simd提供了超宽寄存器，绿茶扫描能很好的利用simd指令加速，因为超宽寄存器能放入更大的bitset，simd也能大大加速bitset之间的位运算

## GO 内存分配
目标：高并发下减少锁竞争，降低小对象分配成本，配合GC提升整体吞吐。

不是每次new/make都向os申请内存，而是走运行时分级管理：mcache->mcentral->mheap，小对象走本地缓存，大对象直接走堆、

### 定义

go runtime为对象分配栈内存或者堆内存，并负责堆内存回收

- 栈上分配：函数局部变量如果生命周期明确，不逃逸，优先分配到goroutine栈上，分配和回收成本低
- 堆上分配：如果对象逃逸，或者大小/生命周期不合适放栈上，就会分配到堆上，由GC回收。

### 原理

1. 栈和堆

- 每个goroutine都要自己的栈，是动态增长的，传统线程是一开始就是很大的固定栈
- 栈上分配快，基本就是移动栈指针
- 函数返回后，栈帧自动回收，不需要GC参与

- 堆是runtime统一管理的共享内存区域
- 堆上对象需要GC标记清除
- 堆分配比栈贵，回收也贵

2. GO 堆分配分层结构
go runtime堆分配器参考了tcmalloc核心思想，核心结构如下
- mcache：线程本地缓存，准确来说是P级别缓存，面向热路径的本地，无锁
  - P是调度和运行时资源管理的逻辑处理器
  - P也可称为上下文，缓存绑定在p上，可以减少线程切换带来的缓存迁移问题
  - 并发分配大多在本地完成，不需要全局锁。
  - 缺点是每个P都要缓存一批span，有一点内存占用，特别是P很多时，本地缓存冗余多
- mcentral：全局某个size class中心缓存，负责给各个mcache分配span，需要加锁，是全局共享。
- mheap：全局堆，更大粒度管理内存，从os申请和返还内存。负责大对象和给mcentral分配span的
- span：内存管理和分配的的核心单元，本质是一段连续页
- arena：堆的实际虚拟地址空间

分配链路
- 小对象分配时，先去P绑定的mcache拿，这里基本无锁
- mcache不够，再去对应size class的mcentral申请一批span
- mcentral不够了，再去mheap申请更大的span
- mheap必要时再向os申请内存



这样的好处
- 大部分小对象分配无需全局锁
- 降低goroutine并发分配时竞争
- 提升吞吐

3. span和size class
go不会对每个对象单独管理，而是按照页和span管理

- page：go 堆管理中的页，通常是8kb
- span：若干page组成的连续内存块
- size class：不同大小对象对应不同规格的分配类别。

span是对象分配的基本管理段位，一个span由多个页组成，绑定某个size class后，会被切分为多个等大的小块。

mspan通常会维护这些元信息：起始地址，页数，对应的spanclass，元素大小，已分配对象数，已分配对象位图，GC标记相关位图，空闲对象索引，是否包含指针等，不仅是内存分配单元，也是GC管理单元。

8B，16B，32B，48B等会映射到不同的size class，某个span会专门分配某个大小类别的小对象。

这样能简化分配逻辑，减少碎片，提高利用率。

4. 小对象和大对象

- 小对象：小于等于32kb，走mcache/mcentral分配
- 大对象：大于32KB，通常直接从mheap分配，按页对其。

小对象数量多，追求速度。大对象少但大，从堆拿更适合，避免打乱小对象分配体系。

5. tiny alloctor
对于一些很小且不包含指针的对象，有一个优化，把多个小对象拼到一个tiny block里分配，减少内存浪费和分配次数

6. 逃逸分析

- 返回局部变量指针
- 局部变量被interface持有，且编译器无法判断其不逃逸
- 闭包引用外部变量
- 超大变量
- 传给未知生命周期的对象



### 内存问题排查

先确认是哪些问题

- 堆对象增长过快？
- GC回收慢？
- 对象长时间被引用无法回收
- goroutine泄露带来的间接内存增长?
- 缓存设计，热点请求，还是临时对象，逃逸等



